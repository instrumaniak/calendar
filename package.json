{
  "name": "calendar-webapp",
  "author": "Raziur Rahman",
  "version": "0.1.0",
  "description": "The calendar web app.",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "engines": {
    "node": "10"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.17.1",
    "mongoose": "^5.6.4",
    "morgan": "^1.9.1"
  }
}
