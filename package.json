{
  "name": "calendar-webapp",
  "author": "Raziur Rahman",
  "version": "0.1.0",
  "description": "The calendar web app.",
  "private": true,
  "scripts": {
    "start": "yarn start:prod",
    "heroku-postbuild": "cd client && yarn && yarn run build && cd ../api && yarn && yarn build"
  },
  "engines": {
    "node": "12",
    "yarn": "1.x"
  }
}
